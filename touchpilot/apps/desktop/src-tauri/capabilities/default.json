{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for Toki overlay, settings, and debug windows",
  "windows": ["overlay", "settings", "debug"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
