{
  "name": "@toki/api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Toki guidance and transcription service. Runs in fixture mode until provider credentials exist.",
  "scripts": {
    "dev": "node --env-file-if-exists=.env --import tsx src/server.ts",
    "typecheck": "tsc --project tsconfig.json",
    "start": "node --env-file-if-exists=.env --import tsx src/server.ts"
  },
  "dependencies": {
    "@toki/shared": "0.1.0",
    "tsx": "^4.22.4"
  },
  "devDependencies": {
    "@types/node": "^25.9.1"
  }
}
