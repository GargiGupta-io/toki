# Phase 13A Legacy Puck Archive

This folder is reference-only. These files are snapshots of the pre-creature puck renderer before the Phase 13A living visual identity work.

Do not import these files from the runtime app. The active renderer should live under `apps/desktop/src` and should be replaced intentionally by the new creature layer.

Archived files:

- `BlobPuck.tsx`
- `BlobCursor.tsx`
- `BlobCursor.css`
- `puckMotion.ts`

Use this archive only to recover behavior or compare against the old implementation.
